{
	"name": "genius",
	"version": "1.0.0",
	"author": "Noah & Kolby",
	"private": true,
	"license": "UNLICENSED",
	"scripts": {
		"next:dev": "yarn --cwd frontend dev",
		"next:build": "yarn --cwd frontend build",
		"next:start": "cd frontend start",
		"next:install": "cd frontend && yarn install",
		"flask:dev": "sh ./scripts/run-flask.sh",
		"dev": "npm-run-all --parallel next:dev flask:dev",
		"start": "cd frontend && yarn start"
	},
	"devDependencies": {
		"dotenv": "^16.3.1",
		"husky": "^4.3.6",
		"isomorphic-fetch": "^3.0.0",
		"npm-run-all": "^4.1.5",
		"prettier": "^2.8.8",
		"pretty-quick": "^3.1.3"
	}
}
